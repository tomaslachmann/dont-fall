{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client", "@testing-library/jest-dom"],
    "noEmit": true
  },
  "include": ["src"],
  // A reference design drop (M9) whose `ui/` kit and `styles/tokens.css` were
  // never committed, so its imports cannot resolve; nothing in the app imports it.
  "exclude": ["src/test_components"]
}
