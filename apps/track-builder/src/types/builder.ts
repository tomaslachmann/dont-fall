/** Impact language. Color is never alone — each kind always carries its glyph and word. */
export type ImpactKind = 'carry' | 'stagger' | 'knockdown';

export type PaletteMode = 'build' | 'compose';
